# Meridian Ashes — Missing Sentence Identification

Offline, from-scratch solution work for the Meridian Ashes challenge.

The challenge data is intentionally excluded from version control. The final
pipeline will train only on `dataset/train.csv`, use `dataset/test.csv` only
for inference, and write `working/submission.csv`.
